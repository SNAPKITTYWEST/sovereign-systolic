# Mistral Large 675B ISA — SnapKitty Deathmatch Round 1

Generated by: `mistral.mistral-large-3-675b-instruct`
Challenge: Pure ISA microcode from scratch until it hits synthesizable Verilog
Time: 8.8s

---

Here are the three required sections for a custom 8-instruction ISA with microcode and synthesizable SystemVerilog:

---

### **ISA SPEC**
**Register File:**
- 8 general-purpose registers (R0-R7), 8-bit wide
- PC (Program Counter), 8-bit
- IR (Instruction Register), 8-bit

**Instruction Format:**
- 8-bit fixed-width instructions
- **Format 1 (Register-Register):** `OOOO RRRR` (4-bit opcode, 4-bit register operand)
- **Format 2 (Immediate):** `OOOO IIII` (4-bit opcode, 4-bit immediate)
- **Format 3 (Branch):** `OOOO AAAA` (4-bit opcode, 4-bit address offset)

**Instructions:**
| Opcode | Mnemonic | Operation                          | Format  |
|--------|----------|------------------------------------|---------|
| 0000   | ADD      | R[dst] = R[dst] + R[src]           | RRRR    |
| 0001   | SUB      | R[dst] = R[dst] - R[src]           | RRRR    |
| 0010   | AND      | R[dst] = R[dst] & R[src]           | RRRR    |
| 0011   | OR       | R[dst] = R[dst] | R[src]           | RRRR    |
| 0100   | LDI      | R[dst] = imm                       | OIII    |
| 0101   | LDR      | R[dst] = MEM[R[src]]               | RRRR    |
| 0110   | STR      | MEM[R[dst]] = R[src]               | RRRR    |
| 0111   | BRZ      | if (R[src] == 0) PC += offset      | OAAAA   |

**Memory:**
- 256-byte address space (8-bit addresses)
- Harvard architecture (separate instruction/data memory)

---

### **MICROCODE TABLE**
**Control Word Fields (16-bit):**
| Bit(s) | Signal          | Description                          |
|--------|-----------------|--------------------------------------|
| 0      | `alu_op[0]`     | ALU operation LSB                    |
| 1      | `alu_op[1]`     | ALU operation MSB                    |
| 2      | `reg_write`     | Enable register write                |
| 3      | `mem_read`      | Enable memory read                   |
| 4      | `mem_write`     | Enable memory write                  |
| 5      | `pc_inc`        | Increment PC                         |
| 6      | `pc_load`       | Load PC from branch target           |
| 7      | `ir_load`       | Load IR from instruction memory      |
| 8      | `src_sel`       | Select source (0=reg, 1=imm)         |
| 9      | `dst_sel`       | Select destination (0=reg, 1=PC)     |
| 10-13  | `imm_src`       | Immediate value source (IR[3:0])     |
| 14-15  | `next_state`    | Next microstate (00=fetch, 01=exec)  |

**Microcode ROM (16 entries, 16-bit words):**
```plaintext
// Fetch cycle (state 0)
0x0001: ir_load=1, pc_inc=1, next_state=01  // Load IR, increment PC
// Execute cycles (state 1)
0x0200: alu_op=00 (ADD), reg_write=1, next_state=00  // ADD
0x0240: alu_op=01 (SUB), reg_write=1, next_state=00  // SUB
0x0280: alu_op=10 (AND), reg_write=1, next_state=00  // AND
0x02C0: alu_op=11 (OR),  reg_write=1, next_state=00  // OR
0x0300: reg_write=1, src_sel=1, imm_src=IR[3:0], next_state=00  // LDI
0x0340: mem_read=1, reg_write=1, next_state=00  // LDR
0x0380: mem_write=1, next_state=00  // STR
0x03C0: pc_load=1, dst_sel=1, next_state=00  // BRZ (conditional)
```

---

### **SYSTEMVERILOG MODULE**
```systemverilog
module cpu (
    input wire clk,
    input wire reset,
    // Memory interfaces
    output wire [7:0] instr_addr,
    input wire [7:0] instr_data,
    output wire [7:0] data_addr,
    output wire [7:0] data_wdata,
    input wire [7:0] data_rdata,
    output wire data_we
);

    // Registers
    reg [7:0] reg_file [0:7];
    reg [7:0] pc, ir;
    reg [1:0] microstate;
    reg [15:0] microcode_rom [0:15];

    // Control signals
    reg [1:0] alu_op;
    reg reg_write, mem_read, mem_write, pc_inc, pc_load, ir_load;
    reg src_sel, dst_sel;
    reg [3:0] imm_src;

    // Internal wires
    wire [7:0] alu_out;
    wire [7:0] src_data = src_sel ? {4'b0, imm_src} : reg_file[ir[3:0]];
    wire [7:0] dst_data = dst_sel ? pc : reg_file[ir[7:4]];

    // ALU
    always_comb begin
        case (alu_op)
            2'b00: alu_out = reg_file[ir[7:4]] + src_data;
            2'b01: alu_out = reg_file[ir[7:4]] - src_data;
            2'b10: alu_out = reg_file[ir[7:4]] & src_data;
            2'b11: alu_out = reg_file[ir[7:4]] | src_data;
        endcase
    end

    // Microcode ROM initialization
    initial begin
        microcode_rom[0]  = 16'b0000000000000001;  // Fetch
        microcode_rom[1]  = 16'b0000001000000000;  // ADD
        microcode_rom[2]  = 16'b0000010001000000;  // SUB
        microcode_rom[3]  = 16'b0000011010000000;  // AND
        microcode_rom[4]  = 16'b0000100011000000;  // OR
        microcode_rom[5]  = 16'b0000101000010000;  // LDI
        microcode_rom[6]  = 16'b0000110000100000;  // LDR
        microcode_rom[7]  = 16'b0000111001000000;  // STR
        microcode_rom[8]  = 16'b0001000000000000;  // BRZ (conditional)
        // Remaining entries unused
        for (int i = 9; i < 16; i++) microcode_rom[i] = 16'b0;
    end

    // Microcode sequencer
    always_ff @(posedge clk or posedge reset) begin
        if (reset) begin
            pc <= 8'b0;
            microstate <= 2'b00;
        end else begin
            case (microstate)
                2'b00: begin  // Fetch
                    ir <= instr_data;
                    pc <= pc + 1;
                    microstate <= 2'b01;
                end
                2'b01: begin  // Execute
                    {alu_op, reg_write, mem_read, mem_write, pc_inc, pc_load, ir_load, src_sel, dst_sel, imm_src} <= microcode_rom[ir[7:4]];
                    if (reg_write) reg_file[ir[7:4]] <= alu_out;
                    if (mem_read) reg_file[ir[7:4]] <= data_rdata;
                    if (mem_write) data_we <= 1;
                    if (pc_load && (reg_file[ir[3:0]] == 0)) pc <= pc + {4'b0, ir[3:0]};
                    microstate <= 2'b00;
                end
            endcase
        end
    end

    // Memory interfaces
    assign instr_addr = pc;
    assign data_addr = reg_file[ir[3:0]];
    assign data_wdata = reg_file[ir[7:4]];

endmodule
```
